# CMake build-output dirs (NOT our build/ scripts directory)
build-speed/
build-multicard/
cmake-build-*/
*.o
*.so
*.a
*.gguf
*.bin

# Models / large data (don't commit weights or corpora)
models/
*.onnx
corpus.jsonl
pairs.jsonl

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
